@extends('layouts.app') @section('title','Reset password | KANTO GOODS') @section('content')<div class="auth-wrap"><form class="card auth-card" method="post" action="{{ route('password.email') }}">@csrf<a class="logo" href="{{ route('home') }}"><b>KG</b><span>KANTO GOODS<small>Account recovery</small></span></a><h1>Password reset</h1><p>Enter a registered email. In local testing mode, the secure reset link appears below.</p><label>Email<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><button class="btn primary wide">Generate reset link</button>@if(session('reset_url'))<div class="reset-link"><small>Testing reset link</small><a href="{{ session('reset_url') }}">Reset this password</a></div>@endif<a class="center" href="{{ route('login') }}">Back to login</a></form></div>@endsection
