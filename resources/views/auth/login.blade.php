@extends('layouts.app') @section('title','Log in | KANTO GOODS') @section('content')<div class="auth-wrap"><form class="card auth-card" method="post" action="{{ route('login.store') }}">@csrf<a class="logo" href="{{ route('home') }}"><b>KG</b><span>KANTO GOODS<small>Secure workspace</small></span></a><h1>Welcome back</h1><p>Use your administrator or cashier account.</p><label>Email<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><label>Password<input type="password" name="password" required></label><label>Workspace<select name="role"><option value="" @selected(empty($selectedRole))>Use account role</option><option value="cashier" @selected(($selectedRole??null)==='cashier')>Cashier</option><option value="admin" @selected(($selectedRole??null)==='admin')>Administrator</option></select></label><label class="check"><input type="checkbox" name="remember"> Keep me signed in</label><button class="btn primary wide">Log in</button><a class="center" href="{{ route('password.request') }}">Forgot password?</a><a class="muted center" href="{{ route('home') }}">Back to home</a></form></div>@endsection
